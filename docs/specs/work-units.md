# Unidades de trabajo

## frontend

Aplicación Next.js en `app/`: componentes de UI, hooks de estado, utilidades de dominio y persistencia en `localStorage` del navegador. No incluye backend ni autenticación.
